services:
  ghostwire:
    build: .
    ports: ["8080:8080"]
    environment:
      GHOSTWIRE_DB: /data/ghostwire.db
      GHOSTWIRE_STATIC: /app/public
    volumes:
      - ghostwire-data:/data
    deploy:
      resources:
        limits:
          memory: 128M
volumes:
  ghostwire-data:
